# Cost Benchmark Fixture

Synthetic .NET 10 and Vue/TypeScript project used only through copies under `.sandbox/runs/<benchmark-id>`

The fixture has no external package dependencies. `BenchmarkChecks` is the authoritative deterministic verifier and must not be modified by Claude benchmark runs
